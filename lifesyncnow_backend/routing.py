# from django.urls import re_path
# from blog import consumers

# websocket_urlpatterns = [
#     re_path(r'^ws/progress/(?P<task_id>\w*)/$',
#             consumers.ProgressWebSocket.as_asgi()),
#     # Add more WebSocket URL patterns if needed
# ]
